<?php

echo 'hello';

if(isset($_POST['addcourse'])) {

    //the patch to the uploaded image.
    $target = "images/.basename($_FILES['image"]['name']);

    //Conectar a la base de datos.

    
}

?>